apiVersion: v1
kind: Service
metadata:
  name: sentinel-db
  labels:
    app: sentinel-db
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "5432"
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: sentinel-db
  type: ClusterIP
